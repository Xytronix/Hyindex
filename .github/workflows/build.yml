name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - uses: gradle/actions/setup-gradle@v4

      - name: Build
        working-directory: hyve-plugin
        run: ./gradlew build

      - name: Shadow JARs
        working-directory: hyve-plugin
        run: ./gradlew :indexer-cli:shadowJar :mcp-server:shadowJar

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: hyindex-jars
          path: |
            hyve-plugin/indexer-cli/build/libs/hyve-knowledge-indexer.jar
            hyve-plugin/mcp-server/build/libs/hyve-knowledge-mcp.jar
